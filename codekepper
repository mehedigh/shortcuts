Mac laravel migrate solution:

Add this in config > database
DB_SOCKET=/Applications/MAMP/tmp/mysql/mysql.sock
Then run on terminal: php artisan optimize OR php artisan config:cache


Install Composer on Mac

Step 1: cd ~
Step 2: sudo mkdir -p /usr/local/bin
Step 3: curl -sS https://getcomposer.org/installer | sudo php -- --install-dir=/usr/local/bin --filename=composer


Use Terminal on Mac

Install Homebrew
Open terminal from a folder
